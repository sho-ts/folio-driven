import { FormContainer } from '@/app/_features/authentication/core/components/blocks';
import { LoginForm } from '@/app/_features/authentication/login/components/blocks';

export const LoginTemplate = () => {
  return (
    <FormContainer>
      <LoginForm />
    </FormContainer>
  );
};
